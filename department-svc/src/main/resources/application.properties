spring.application.name=department-svc
##Configure SQL database
#spring.datasource.url=jdbc:mysql://localhost:3306/department_db
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:department_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Sumit@SQL}
#
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
#spring.jpa.hibernate.ddl-auto=update
#
#eureka.instance.client.serverUrl.defaultZone=http://localhost:8761/eureka/
eureka.client.service-url.defaultZone=http://${EUREKA_HOST:localhost}:8761/eureka/
# spring.config.import=optional:configserver:http://localhost:8888
spring.config.import=optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:8888
managements.endpoints.web.exposure.include=*

# spring.rabbitmq.host=localhost
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

spring.boot.message=Hello, Department Service - this is used to test cloud bus test
